# Diario Auto V8.1 — Cloudflare Workers

Worker: `auto`

Static Assets: `./dist`

SPA fallback: `single-page-application`

Deploy: `npm run deploy`

Prima del Play Store sostituire il fingerprint placeholder in `public/.well-known/assetlinks.json` con quello reale della chiave Android.
