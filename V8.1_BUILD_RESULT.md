# Diario Auto V8.3 — Build

Correzioni applicate ai due errori TS6133 segnalati da Cloudflare:
- `Dashboard.tsx`: rimosso il parametro `maintenance` inutilizzato da `urgentTotal`.
- `VehicleDetail.tsx`: rimosso `totalFuelLiters`, dichiarato ma mai utilizzato.

La build locale è stata avviata con `npm ci` e `npm run build`, ma l'ambiente di esecuzione ha raggiunto il limite di tempo prima di produrre un esito definitivo. Il log Cloudflare resta il riferimento per il problema originale.
